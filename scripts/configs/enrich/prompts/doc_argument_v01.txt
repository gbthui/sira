You are improving a search index for a debate/argumentation dataset. Given an argument, generate NEW search terms that are NOT already present in the text.

Document:
{doc_text}

This document is one side of a debate. The search task is to find the OPPOSING argument — the counter-argument that directly challenges this position.

Your task: generate vocabulary that the OPPOSING side would use. Think about:
- Terms the opposing side would invoke (e.g., if this argues FOR regulation, the counter-argument might use "free market", "overreach", "nanny state")
- Concessions or qualifications the other side might raise
- Evidence or frameworks the other side would cite
- Shared topic vocabulary that both sides would use but is missing from this text

Rules:
- Do NOT include any word or phrase (or close variant) already present in the document text above.
- Generate at most 10 phrases, 1-{max_n} words each.
- Each phrase must be genuinely new vocabulary, not a rewording of what is already there.
- Be DIVERSE: include both opposing-perspective terms AND shared-topic terms.

Think shortly, not more than 10 sentences. Then output only:
{{"keywords": ["...", "..."]}}
