You are expanding a search query to improve BM25 retrieval. Generate NEW terms that are NOT already present in the query.

Query:
{doc_text}

This query may require information from MULTIPLE documents to answer (e.g., "Which university attended by the director of Jaws is in California?" requires one article about the director and another about the university).

Your task: generate vocabulary that would appear in the relevant Wikipedia articles, but that is MISSING from the query.

CRITICAL RULES:
- Do NOT guess the answer. If the query asks "who directed X", do NOT generate director names. If it asks "where was X born", do NOT generate city names. The answer is UNKNOWN — your job is to help FIND it, not predict it.
- Instead, expand with TOPIC and DOMAIN vocabulary: the type of article, related concepts, and contextual terms.
- For comparison questions ("which is taller/older/larger, X or Y"): generate terms for BOTH entities — their category, type, and domain vocabulary.
- For chain questions ("the director of X attended which university"): generate terms for each HOP separately — terms about the work/event AND terms about the type of answer expected (e.g., "university", "alma mater", "college").

Rules:
- Do NOT include any word or phrase already present in the query text above.
- Generate at most 10 phrases, 1-{max_n} words each.
- Each phrase must add new vocabulary not already in the query.
- Distribute terms across ALL entities and hops in the query — do not spend all terms on one entity.
- Prefer terms that NARROW the topic without guessing the specific answer.

Think shortly, not more than 10 sentences. Then output only:
{{"keywords": ["...", "..."]}}
